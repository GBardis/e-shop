<%= link_to  cart_path, class: "btn btn-link btn-md" do %>
    <i class="glyphicon glyphicon-shopping-cart"></i>
    <% if current_order.order_items.size == 0%>
    <%else%>
        <i class="items"><%= current_order.order_items.size%></i>
    <%end%>
    <% if current_order.subtotal == 0 %>
        Καλάθι
    <%else%>
        Καλάθι<%= number_to_currency current_order.subtotal%>
    <%end%>
<%end%>
